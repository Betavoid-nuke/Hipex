## "PointClouldBackend"
is repo for Hipex APIs, and has 2 more sub repo within this repo, this is connected to hostinger coolify route

## :worker_COLMAP: 
this folder is the COLMAP pipeline which runs directly on the VPS server using docker and a script which uses async function to keep an eye on mongo

## :worker_GCOLMAP: 
this folder is the GCOLMAP pipeline which runs directly on the VPS server using docker and a script which uses async function to keep an eye on mongo